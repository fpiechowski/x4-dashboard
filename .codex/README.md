# Codex Workspace Guide

This folder contains the Codex-native operational setup for this repository.

- `playbook-*.md` defines role responsibilities.
- `workflow-*.md` defines repeatable task execution patterns.

Use this folder together with `AGENTS.md`.
`AGENTS.md` remains the top-level source of truth for repository rules.
